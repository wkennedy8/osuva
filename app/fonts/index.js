import { Inter, Roboto_Mono, Onest, League_Spartan } from 'next/font/google';

export const inter = Inter({
	subsets: ['latin'],
	display: 'swap'
});

export const roboto_mono = Roboto_Mono({
	subsets: ['latin'],
	display: 'swap'
});

export const onest = Onest({
	subsets: ['latin'],
	display: 'swap'
});

export const league_spartan = League_Spartan({
	subsets: ['latin'],
	display: 'swap'
});
